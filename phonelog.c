#include <stdio.h>
#include <strg.h>
struct nu(){
    char name[50];
    int num;
};
char menu(){

    printf("would u like to view the phone book? (y or n)\n");
    if (scanf(%c)=y){
        //disp the log book
        printf("--------------------\n",nu.name,nu.num);
    }
    else{ main();
    }

}
int main(){}
    struct nu logs;
    printf("enter the name\n");
    scanf("%s\n",&logs.name);
    printf("enter the phone number\n");
    scanf("%d\n",&logs.num);
    menu(logs);

}